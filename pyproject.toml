[tool.poetry]
name = "methyldl"
version = "0.4.5"
description = ""
authors = ["Dmytro Rizdvanteskyi"]
readme = "README.md"
packages = [{include = "methyldl"}]

[tool.poetry.dependencies]
python = "^3.10"
numpy = "2.0.2"
transformers = "4.52.4"
torch = {version = "^2.7.0+cu128", source = "torch128"}
scikit-learn = "^1.5.2"
einops = "^0.8.0"
torchvision = "^0.22"
accelerate = ">=0.26.0"
pandas = "^2.2.3"
matplotlib = "^3.10.0"
pyarrow = "19.0.1"
mlflow = "^2.22.1"
seaborn = "^0.13.2"
gputil = "^1.4.0"
xgboost = "^3.1.3"
umap-learn = "^0.5.11"
pysam = "^0.23.3"


[tool.poetry.group.dev.dependencies]
ipykernel = "^6.29.5"
coverage = "^7.10.7"
parameterized = "^0.9.0"
genbadge ="^1.1.0"
defusedxml ="^0.7.0"

[tool.poetry.extras]
eda = ["intervaltree"]

[[tool.poetry.source]]
name = "torch128"
url = "https://download.pytorch.org/whl/cu128"
priority = "explicit"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.coverage.run]
source_pkgs = ["methyldl"]
omit = [
    "*/tests/*",
    "*/test_*",
    "*/__init__.py",
    "*/tmp/*",
    "/tmp/*",
    "*/_remote_module_non_scriptable.py",
    "*/site-packages/*",
    "*/.venv/*",
    "*/venv/*",
]
disable_warnings = ["no-data-collected", "module-not-measured"]
command_line = "-m unittest discover -v"

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise AssertionError",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
    "if TYPE_CHECKING:",
]
precision = 2
skip_empty = false
ignore_errors = true  # This helps with the temp file issue
skip_covered = false  # Also show fully covered files
show_missing = false  # Show which lines are missing

[tool.coverage.html]
directory = "htmlcov"
show_contexts = true